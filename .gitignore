# LaTeX中間ファイルを除外
*.aux
*.bbl
*.blg
*.dvi
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.synctex.gz
*.nav
*.snm
*.vrb

# outディレクトリ全体を除外（PDFは個別に管理）
out/
!out/*.pdf

# システムファイルを除外
.DS_Store

# 他の環境で必要なファイルは除外しない：
# - *.tex (LaTeXソースファイル)
# - *.bib (参考文献ファイル) 
# - .latexmkrc (latexmk設定)
# - .vscode/ (VS Code設定)
